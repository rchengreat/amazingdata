[project]
name = "amazingdata-fetcher"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "pyarrow",
    "pandas",
    "python-dotenv",
    "loguru",
]

[tool.pip.find-links]
wheel = "./wheel"
