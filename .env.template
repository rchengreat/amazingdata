# ── AmazingData SDK 凭证 ─────────────────────────────────────
AD_HOST=101.230.159.234
AD_PORT=8600
AD_USERNAME=CHANGE_ME
AD_PASSWORD=CHANGE_ME

# ── 数据输出路径 ─────────────────────────────────────────────
# 测试阶段（NAS 本机运行，写入 amazingdata 目录）：
OUTPUT_DIR=/volume1/amazingdata/data

# 正式接入 qa 项目后切换为：
# OUTPUT_DIR=/volume1/tgw

# SDK 本地缓存目录（复权因子、历史代码表等接口需要）：
SDK_CACHE_DIR=/volume1/amazingdata/sdk_cache

# ── 日志 ─────────────────────────────────────────────────────
LOG_LEVEL=INFO
LOG_DIR=./logs
